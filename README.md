# 🦯 Blind Stick Alert System (ESP32 + Twilio + Node.js)

This project alerts a caretaker via **SMS (Twilio)** when a fall event is detected from an IoT-enabled blind stick device.

## ⚙️ Features
- ESP32 sends event data to Node.js server
- Events are saved in `events.json`
- SMS alert via Twilio on fall detection
- Web dashboard to view recent events

## 🖥️ Run Locally
1. Clone this repository  
   ```bash
   git clone https://github.com/your-username/blindStick.git
   cd blindStick
